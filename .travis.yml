language: python
python:
  - "3.6"
install:
  - "pip install -r requirements.txt"
  - "pip install coveralls"
  - "pip install ."
script:
  - pytest --cov=draco
  - ansunit asp/tests.yaml
addons:
  apt:
    packages:
    - gringo
after_success:
    - coveralls
