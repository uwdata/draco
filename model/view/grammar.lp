% marks
mark_type(bar;line;point).

% data types
type_type(quantitative;nominal).

% channels
channel_type(x;y;color).

% aggregations
aggregate_type(count;mean).

% bin (boolean)
bin_type(true;false).
