% marks
mark_type(bar;line;point;tick).

% data types
type_type(quantitative;ordinal;nominal;temporal).

% channels
channel_type(x;y;color;size).

% aggregations
aggregate_type(count;mean;sum).

% bin (boolean)
bin_type(true;false).
